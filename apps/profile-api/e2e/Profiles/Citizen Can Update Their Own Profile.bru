meta {
  name: Citizen can update their own profile
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/profiles/:profileId/:organizationId
  body: json
  auth: none
}

params:path {
  profileId: {{profileId}}
  organizationId: first-testing
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{citizenToken}}
}

body {
  {
    "phone": "11223344"
  }
}

assert {
  res.status: eq 200
  res.body.data.id: eq {{profileId}}
}