meta {
  name: Public Servant Can Find A Profile
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/profiles/find-profile?lastName=Belusho
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:post-response {
  userId: res.body.data && res.body.data[0] ?  res.body.data[0].id : undefined
}

assert {
  res.status: eq 200
  res.body.data: isDefined
  res.body.data[0]: isDefined
  res.body.data[0].id: isDefined
}